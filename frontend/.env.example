# Backend API Configuration (Server-side only - not exposed to browser)
# This is used by Next.js API routes to proxy requests to the FastAPI backend
BACKEND_URL=http://localhost:8000

# WebSocket Configuration (Client-side - for real-time updates)
# Use ws:// for development, wss:// for production
NEXT_PUBLIC_WEBSOCKET_BASE_URL=ws://localhost:8000
